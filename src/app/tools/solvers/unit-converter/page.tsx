import type { Metadata } from "next";
import { UnitConverterSolver } from "./client";
export const metadata: Metadata = { title: "unit-converter solver — 3DBuildBot" };
export default function Page() { return <UnitConverterSolver />; }
