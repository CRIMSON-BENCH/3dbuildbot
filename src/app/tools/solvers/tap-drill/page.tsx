import type { Metadata } from "next";
import { TapDrillSolver } from "./client";
export const metadata: Metadata = { title: "tap-drill solver — 3DBuildBot" };
export default function Page() { return <TapDrillSolver />; }
