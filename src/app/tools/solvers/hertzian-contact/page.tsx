import type { Metadata } from "next";
import { HertzianSolver } from "./client";
export const metadata: Metadata = { title: "hertzian-contact solver — 3DBuildBot" };
export default function Page() { return <HertzianSolver />; }
